__pycache__/
*.py[cod]
# Runtime data that may contain secrets
data.json
.env
data.enc
data.salt

# Executables and build artifacts
*.exe
dist/
build/

# IDE/editor folders
.vscode/

# Python virtual env
venv/
env/

# OS files
.DS_Store
Thumbs.db

# IMPORTANT: Do NOT store tokens or secrets in tracked files.
# If you need to run GitHub API commands, set `GITHUB_TOKEN` in your shell temporarily and
# do NOT commit it. Example (run locally, do not commit):
#
#   # set GITHUB_TOKEN in your shell (do not commit it) and run API commands locally